{
  "name": "fish-registry",
  "private": true,
  "scripts": {
    "api": "cd backend && mvnw.cmd -q spring-boot:run",
    "web": "cd frontend && ng serve --proxy-config proxy.conf.json",
    "dev": "concurrently -n api,web -c auto \"npm:api\" \"npm:web\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
